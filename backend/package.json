{
  "name": "fold-commons-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker backend for the fold-commons community image library (R2-backed).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "wrangler": "latest"
  }
}
